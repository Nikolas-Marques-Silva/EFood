import styled from 'styled-components'
import { colors } from '../../styles'

export const Title = styled.h1`
  color: ${colors.pink};
  font-size: 38px;
  font-weight: 700;
  line-height: 42px;
  text-align: center;
  padding-top: 64px;
  padding-bottom: 40px;
  margin: 0;
`
